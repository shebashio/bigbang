# Example only: replace every placeholder and encrypt/manage this Secret before
# deployment. Keep this configuration aligned with Thanos upstream.objstoreConfig.
apiVersion: v1
kind: Secret
metadata:
  name: monitoring-objstore-secret
  namespace: monitoring
type: Opaque
stringData:
  objstore.yml: |-
    type: s3
    config:
      bucket: REPLACE_ME
      endpoint: REPLACE_ME
      access_key: REPLACE_ME
      secret_key: REPLACE_ME
      insecure: false
