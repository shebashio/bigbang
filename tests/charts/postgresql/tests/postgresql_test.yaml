suite: PostgreSQL CI helper
templates:
  - templates/cluster.yaml
  - templates/readiness.yaml
  - templates/authorizationpolicy.yaml
tests:
  - it: creates PostgreSQL 17 with GitLab initialization
    template: templates/cluster.yaml
    asserts:
      - isKind:
          of: Cluster
      - equal:
          path: metadata.name
          value: postgresql
      - equal:
          path: spec.imageName
          value: registry1.dso.mil/ironbank/opensource/postgres/postgresql:17.10
      - equal:
          path: spec.bootstrap.initdb.database
          value: gitlabhq_production
      - equal:
          path: spec.bootstrap.initdb.owner
          value: gitlab
      - notExists:
          path: spec.bootstrap.initdb.secret
      - contains:
          path: spec.bootstrap.initdb.postInitApplicationSQL
          content: CREATE EXTENSION IF NOT EXISTS pg_trgm;

  - it: gates Flux readiness on live PostgreSQL connectivity with CNPG-generated credentials
    template: templates/readiness.yaml
    asserts:
      - isKind:
          of: Deployment
      - equal:
          path: metadata.name
          value: postgresql-readiness
      - equal:
          path: spec.template.metadata.labels["app.kubernetes.io/version"]
          value: "17"
      - equal:
          path: spec.template.spec.automountServiceAccountToken
          value: false
      - matchRegex:
          path: spec.template.spec.containers[0].args[1]
          pattern: (?s)postgresql-rw.*SELECT 1
      - equal:
          path: spec.template.spec.containers[0].env[0].valueFrom.secretKeyRef.name
          value: postgresql-app
      - equal:
          path: spec.template.spec.containers[0].env[0].valueFrom.secretKeyRef.key
          value: username
      - equal:
          path: spec.template.spec.containers[0].env[1].valueFrom.secretKeyRef.name
          value: postgresql-app
      - equal:
          path: spec.template.spec.containers[0].env[1].valueFrom.secretKeyRef.key
          value: password
      - equal:
          path: spec.template.spec.containers[0].env[2].value
          value: gitlabhq_production
      - equal:
          path: spec.template.spec.containers[0].readinessProbe.exec.command[2]
          value: test -f /ready/connected

  - it: follows the CNPG application Secret naming convention for a renamed cluster
    template: templates/readiness.yaml
    set:
      fullnameOverride: gitlab-postgresql
    asserts:
      - equal:
          path: spec.template.spec.containers[0].env[0].valueFrom.secretKeyRef.name
          value: gitlab-postgresql-app
      - equal:
          path: spec.template.spec.containers[0].env[1].valueFrom.secretKeyRef.name
          value: gitlab-postgresql-app

  - it: omits ambient authorization when ambient mode is disabled
    template: templates/authorizationpolicy.yaml
    asserts:
      - hasDocuments:
          count: 0

  - it: authorizes only the ambient GitLab, readiness, and CNPG operator clients
    template: templates/authorizationpolicy.yaml
    set:
      istio.ambient.enabled: true
    asserts:
      - isKind:
          of: AuthorizationPolicy
      - equal:
          path: metadata.name
          value: postgresql-ambient-clients
      - equal:
          path: spec.selector.matchLabels["cnpg.io/cluster"]
          value: postgresql
      - equal:
          path: spec.rules[0].from[0].source.namespaces
          value:
            - NAMESPACE
            - gitlab
      - equal:
          path: spec.rules[0].to[0].operation.ports
          value:
            - "5432"
      - equal:
          path: spec.rules[1].from[0].source.principals
          value:
            - cluster.local/ns/cloudnative-pg/sa/cloudnative-pg
      - equal:
          path: spec.rules[1].to[0].operation.ports
          value:
            - "8000"
