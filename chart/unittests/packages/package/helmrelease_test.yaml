# yaml-language-server: $schema=https://raw.githubusercontent.com/helm-unittest/helm-unittest/main/schema/helm-testsuite.json
suite: Packages - Package - HelmRelease Tests
templates:
  - templates/package/helmrelease.yaml
release:
  name: test
set:
  packages:
    sample:
      enabled: true
      git:
        repo: https://example.com/sample.git
        path: chart
        tag: 1.0.0
tests:
  - it: should include common.yaml valuesFrom when bbCommonValues is enabled
    set:
      packages:
        sample:
          bbCommonValues: true
    asserts:
      - contains:
          path: spec.valuesFrom
          content:
            name: sample-values
            kind: Secret
            valuesKey: common.yaml

  - it: should include common.yaml valuesFrom when bbCommonValues is true
    set:
      packages:
        sample:
          enabled: true
          bbCommonValues: true
          git:
            repo: https://example.com/sample.git
            path: chart
            tag: 1.0.0
    asserts:
      - contains:
          path: spec.valuesFrom
          content:
            name: sample-values
            kind: Secret
            valuesKey: common.yaml

  - it: should not include common.yaml valuesFrom when bbCommonValues is false
    set:
      packages:
        sample:
          enabled: true
          bbCommonValues: false
          git:
            repo: https://example.com/sample.git
            path: chart
            tag: 1.0.0
    asserts:
      - notContains:
          path: spec.valuesFrom
          content:
            name: sample-values
            kind: Secret
            valuesKey: common.yaml

  - it: should always include package values Secret in valuesFrom
    set:
      packages:
        sample:
          enabled: true
          bbCommonValues: false
          git:
            repo: https://example.com/sample.git
            path: chart
            tag: 1.0.0
    asserts:
      - contains:
          path: spec.valuesFrom
          content:
            name: sample-values
            kind: Secret
