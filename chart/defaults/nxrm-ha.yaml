# Default configuration for nxrm-ha package

# Package values (merged into package config)
package: {}

# Note: postRenderers are defined in templates/package/_postrenderers.tpl
# using native Helm templating instead of __PKG__ placeholder replacement
