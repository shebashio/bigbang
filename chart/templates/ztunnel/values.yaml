{{- $pkg := "ztunnel" }}

{{- /* Create secret */ -}}
{{- if or .Values.ztunnel.enabled .Values.istio.ambient.enabled }}
{{- include "values-secret" (dict "root" $ "package" (get .Values $pkg) "name" "ztunnel" "defaults" (include "bigbang.defaults.ztunnel" .)) }}
{{- end }}

{{- define "bigbang.defaults.ztunnel" -}}
upstream:
  ambient:
    enabled: {{ or .Values.istio.ambient.enabled .Values.ztunnel.enabled }}
  global:
    networkPolicy:
      enabled: true
    imagePullPolicy: {{ .Values.imagePullPolicy }}
    {{- include "secretsImagePullSecrets" . | nindent 4 }}
{{- end }}
