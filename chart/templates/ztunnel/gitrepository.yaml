{{- if and (eq .Values.ztunnel.sourceType "git") (not .Values.offline) (or .Values.ztunnel.enabled .Values.ambient) }}
{{- include "bigbang.gitRepository" (dict "name" "ztunnel" "gitCredsName" "ztunnel" "git" .Values.ztunnel.git "component" "core" "root" .) }}
{{- end }}
