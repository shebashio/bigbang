{{- if and (eq .Values.istioZtunnel.sourceType "git") (not .Values.offline) .Values.istioZtunnel.enabled }}
{{- $gitCredsDict := dict
  "name" "istioZtunnel"
  "packageGitScope" .Values.istioZtunnel.git
  "rootScope" .
  "releaseName" .Release.Name
}}
apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: istio-ztunnel
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/name: istio-ztunnel
    app.kubernetes.io/component: "core"
    {{- include "commonLabels" . | nindent 4}}
spec:
  interval: {{ .Values.flux.interval }}
  url: {{ .Values.istioZtunnel.git.repo }}
  ref:
    {{- include "validRef" .Values.istioZtunnel.git | nindent 4 }}
  {{ include "gitIgnore" . }}
  {{- include "gitCredsExtended" $gitCredsDict | nindent 2 }}
{{- end }}
