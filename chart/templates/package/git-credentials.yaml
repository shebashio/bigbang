{{- /* Used for GitOps on a package's Helm chart */ -}}
{{- range $pkg, $vals := .Values.packages -}}
{{- if and (dig "enabled" true $vals) (eq (dig "sourceType" "git" $vals) "git") ($vals.git) -}}
{{- $pkg = include "resourceName" $pkg -}}
{{- $namespace := dig "namespace" "name" $pkg $vals }}
{{- if dig "helmRelease" "namespace" nil $vals -}}
{{- $namespace = dig "helmRelease" "namespace" "" $vals -}}
{{- end -}}
{{- $gitCredsSecretDict := dict
  "name" $pkg
  "targetScope" $vals
  "releaseName" $pkg
  "releaseNamespace" $namespace
}}
{{- include "gitCredsSecret" $gitCredsSecretDict | nindent 0 }}
---
{{- end -}}
{{- end -}}
