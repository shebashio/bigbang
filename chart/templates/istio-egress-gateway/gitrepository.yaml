{{- include "bigbang.normalizePackageAliases" . -}}
{{- if and .Values.istioEgressGateway.enabled (eq (include "ambientEnabled" .) "true") (eq .Values.istioEgressGateway.sourceType "git") (not .Values.offline) }}
{{- include "bigbang.gitRepository" (dict "name" "istio-egress-gateway" "gitCredsName" "istioEgressGateway" "git" .Values.istioEgressGateway.git "component" "core" "root" .) }}
{{- end }}
