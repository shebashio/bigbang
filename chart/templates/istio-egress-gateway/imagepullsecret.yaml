{{- include "bigbang.normalizePackageAliases" . -}}
{{- /* The chart deploys no workloads, but istiod creates the waypoint pods in
       this namespace and they pull the proxy image from registry1. */ -}}
{{- if and .Values.istioEgressGateway.enabled (eq (include "ambientEnabled" .) "true") }}
{{- include "bigbang.imagePullSecret" (dict "root" . "namespace" "istio-egress" "appName" "istio-egress-gateway" "component" "core") }}
{{- end }}
