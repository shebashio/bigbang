{{- if and (eq .Values.istio3.sourceType "git") (not .Values.offline) .Values.istio3.enabled }}
{{- $gitCredsDict := dict
  "name" "istio3"
  "packageGitScope" .Values.istio3.git
  "rootScope" .
  "releaseName" .Release.Name
}}
apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: istio3
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/name: istio3
    app.kubernetes.io/component: "core"
    {{- include "commonLabels" . | nindent 4}}
spec:
  interval: {{ .Values.flux.interval }}
  url: {{ .Values.istio3.git.repo }}
  ref:
    {{- include "validRef" .Values.istio3.git | nindent 4 }}
  {{ include "gitIgnore" . }}
  {{- include "gitCredsExtended" $gitCredsDict | nindent 2 }}
{{- end }}
