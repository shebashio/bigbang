{{- $fluxSettingsMonitoring := merge .Values.addons.grafanaAlloy.flux .Values.flux -}}
{{- if .Values.addons.grafanaAlloy.enabled }}
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: grafana-alloy
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/name: grafana-alloy
    {{- include "commonLabels" . | nindent 4}}
  annotations:
    checksum/bigbang-values: {{ include (print $.Template.BasePath "/grafana-alloy/values.yaml") . | sha256sum }}
spec:
  targetNamespace: grafana-alloy
  chart:
    spec:
      {{- if eq .Values.addons.grafanaAlloy.sourceType "git" }}
      chart: {{ .Values.addons.grafanaAlloy.git.path }}
      sourceRef:
        kind: GitRepository
        name: grafana-alloy
        namespace: {{ .Release.Namespace }}
      {{- else }}
      chart: {{ .Values.addons.grafanaAlloy.helmRepo.chartName }}
      version: {{ .Values.addons.grafanaAlloy.helmRepo.tag }}
      sourceRef:
        kind: HelmRepository
        name: {{ .Values.addons.grafanaAlloy.helmRepo.repoName }}
        namespace: {{ .Release.Namespace }}
      {{- $repoType := include "getRepoType" (dict "repoName" .Values.addons.grafanaAlloy.helmRepo.repoName "allRepos" $.Values.helmRepositories) -}}
      {{- if (and .Values.addons.grafanaAlloy.helmRepo.cosignVerify (eq $repoType "oci")) }} # Needs to be an OCI repo
      verify:
        provider: cosign
        secretRef:
          name: {{ printf "%s-cosign-pub" .Values.addons.grafanaAlloy.helmRepo.repoName }}
      {{- end }}
      {{- end }}
      interval: 5m

  {{- toYaml $fluxSettingsMonitoring | nindent 2 }}

  {{- if .Values.addons.grafanaAlloy.postRenderers }}
  postRenderers:
  {{ toYaml .Values.addons.grafanaAlloy.postRenderers | nindent 4 }}
  {{- end }}
  valuesFrom:
    - name: {{ .Release.Name }}-grafana-alloy-values
      kind: Secret
      valuesKey: "common"
    - name: {{ .Release.Name }}-grafana-alloy-values
      kind: Secret
      valuesKey: "defaults"
    - name: {{ .Release.Name }}-grafana-alloy-values
      kind: Secret
      valuesKey: "overlays"

  # TODO: DRY this up
  {{- if or .Values.gatekeeper.enabled .Values.istio.enabled .Values.kyvernoPolicies.enabled .Values.monitoring.enabled }}
  dependsOn:
  {{- if .Values.istio.enabled }}
    - name: istio
      namespace: {{ .Release.Namespace }}
  {{- end }}
  {{- if .Values.gatekeeper.enabled }}
    - name: gatekeeper
      namespace: {{ .Release.Namespace }}
  {{- end }}
  {{- if .Values.kyvernoPolicies.enabled }}
    - name: kyverno-policies
      namespace: {{ .Release.Namespace }}
  {{- end }}
  {{- end }}
{{- end }}

