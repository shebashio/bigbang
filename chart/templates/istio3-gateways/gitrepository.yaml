{{- if and (eq .Values.istio3Gateways.sourceType "git") (not .Values.offline) .Values.istio3Gateways.enabled }}
{{- $gitCredsDict := dict
  "name" "istio3Gateways"
  "packageGitScope" .Values.istio3Gateways.git
  "rootScope" .
  "releaseName" .Release.Name
}}
apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: istio-gateways
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/name: istio-gateways
    app.kubernetes.io/component: "core"
    {{- include "commonLabels" . | nindent 4}}
spec:
  interval: {{ .Values.flux.interval }}
  url: {{ .Values.istio3Gateways.git.repo }}
  ref:
    {{- include "validRef" .Values.istio3Gateways.git | nindent 4 }}
  {{ include "gitIgnore" . }}
  {{- include "gitCredsExtended" $gitCredsDict | nindent 2 }}
{{- end }}
