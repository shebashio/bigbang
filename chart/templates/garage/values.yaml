{{- if .Values.addons.garage.enabled }}
{{- $garagePackage := deepCopy .Values.addons.garage -}}
{{- if .Values.addons.garage.values -}}
{{- $cleanedValues := include "mergeLegacyIstioHardenedKeys" (dict "values" .Values.addons.garage.values) | fromYaml -}}
{{- $garagePackage = set $garagePackage "values" $cleanedValues -}}
{{- end -}}
{{- include "values-secret" (dict
  "root" $
  "package" $garagePackage
  "name" "garage"
  "defaults" (include "bigbang.defaults.garage" .)
)}}
{{- end }}

{{- define "bigbang.defaults.garage" -}}
{{- $istioEnabled := eq (include "istioEnabled" .) "true" }}
{{- $istioHardened := dig "hardened" "enabled" false .Values.istiod.values }}
# hostname is deprecated and replaced with domain. But if hostname exists then use it.
{{- $domainName := default .Values.domain .Values.hostname }}

bb-common:
  domain: {{ $domainName }}
  istio:
    enabled: {{ $istioEnabled }}
    ambient:
      enabled: {{ include "ambientEnabled" . }}
    sidecar:
      enabled: {{ and $istioHardened (ne (include "ambientEnabled" .) "true") }}
    authorizationPolicies:
      enabled: {{ $istioHardened }}
      generateFromNetpol: {{ $istioHardened }}
  networkPolicies:
    enabled: {{ .Values.networkPolicies.enabled }}
    hbonePortInjection:
      enabled: {{ include "ambientEnabled" . }}
    egress:
      definitions: {{ .Values.networkPolicies.egress.definitions | toYaml | nindent 8 }}
      from:
        garage:
          to:
            k8s:
              tempo/tempo:9411: {{ .Values.tempo.enabled }}
    ingress:
      definitions: {{ .Values.networkPolicies.ingress.definitions | toYaml | nindent 8 }}
  routes:
    inbound:
      s3-api:
        enabled: true
        gateways:
        - {{ include "getGatewayName" (dict "gateway" (.Values.addons.garage.ingress.gateway | default "public") "root" .)}}
        hosts:
        - garage-s3.{{ $domainName }}
        service: garage
        port: 3900
      s3-web:
        enabled: false
        gateways:
        - {{ include "getGatewayName" (dict "gateway" (.Values.addons.garage.ingress.gateway | default "public") "root" .)}}
        hosts:
        - garage-web.{{ $domainName }}
        service: garage
        port: 3902
    outbound:
      bb-tests:
        enabled: {{ dig "bbtests" "enabled" false .Values.addons.garage.values }}
        hosts:
        - repo1.dso.mil

monitoring:
  enabled: {{ .Values.monitoring.enabled }}

{{- $garageInitEnabled := .Values.addons.garage.garageInit.enabled }}
{{- $garageAdminToken := "" }}
{{- if $garageInitEnabled }}
{{- $garageAdminToken = required "addons.garage.garageInit.adminToken is required when garageInit.enabled is true; generate with: openssl rand -hex 32" .Values.addons.garage.garageInit.adminToken }}
{{- end }}

upstream:
  image:
    pullPolicy: {{ .Values.imagePullPolicy }}
  {{- include "secretsImagePullSecretsWithName" . | nindent 2 }}
  monitoring:
    metrics:
      enabled: {{ eq (include "metricScrapingEnabled" .) "true" }}
      serviceMonitor:
        enabled: {{ eq (include "metricScrapingEnabled" .) "true" }}
  {{- if $garageInitEnabled }}
  environment:
    - name: GARAGE_ADMIN_TOKEN
      value: {{ $garageAdminToken | quote }}
  {{- end }}

garageInit:
  enabled: {{ $garageInitEnabled }}
  {{- if $garageInitEnabled }}
  credentials:
    accessKeyId: {{ .Values.addons.garage.garageInit.credentials.accessKeyId | quote }}
    secretAccessKey: {{ .Values.addons.garage.garageInit.credentials.secretAccessKey | quote }}
  buckets: {{ .Values.addons.garage.garageInit.buckets | toYaml | nindent 4 }}
  adminToken: {{ $garageAdminToken | quote }}
  {{- end }}
  image:
    pullPolicy: {{ .Values.imagePullPolicy }}

{{- end -}}
