{{- $fluxSettingsGitlab := merge .Values.packages.addons.gitlab.flux .Values.flux -}}
{{- if .Values.packages.addons.gitlab.enabled }}
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: gitlab
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/name: gitlab
    app.kubernetes.io/component: "developer-tools"
    {{- include "commonLabels" . | nindent 4}}
  annotations:
    checksum/bigbang-values: {{ include (print $.Template.BasePath "/gitlab/values.yaml") . | sha256sum }}
spec:
  releaseName: gitlab
  targetNamespace: gitlab
  chart:
    spec:
      {{- if eq .Values.packages.addons.gitlab.sourceType "git" }}
      chart: {{ .Values.packages.addons.gitlab.git.path | default "./chart" }}
      sourceRef:
        kind: GitRepository
        name: gitlab
        namespace: {{ .Release.Namespace }}
      {{- else }}
      chart: {{ .Values.packages.addons.gitlab.helmRepo.chartName }}
      version: {{ .Values.packages.addons.gitlab.helmRepo.tag }}
      sourceRef:
        kind: HelmRepository
        name: {{ .Values.packages.addons.gitlab.helmRepo.repoName }}
        namespace: {{ .Release.Namespace }}
      {{- $repoType := include "getRepoType" (dict "repoName" .Values.packages.addons.gitlab.helmRepo.repoName "allRepos" $.Values.helmRepositories) -}}
      {{- if (and .Values.packages.addons.gitlab.helmRepo.cosignVerify (eq $repoType "oci")) }} # Needs to be an OCI repo
      verify:
        provider: cosign
        secretRef:
          name: {{ printf "%s-cosign-pub" .Values.packages.addons.gitlab.helmRepo.repoName }}
      {{- end }}
      {{- end }}
      interval: 5m


  {{- toYaml $fluxSettingsGitlab | nindent 2 }}
  {{- if or .Values.packages.addons.gitlab.postRenderers .Values.packages.addons.monitoring.enabled }}
  postRenderers:
  {{- if .Values.packages.addons.monitoring.enabled }}
  {{ include "gitlab.serviceMonitorPostRenderers" . | nindent 4 }}
  {{- end }}
  {{- with .Values.packages.addons.gitlab.postRenderers }}
  {{ toYaml . | nindent 4 }}
  {{- end }}
  {{- end }}
  valuesFrom:
    - name: {{ .Release.Name }}-gitlab-values
      kind: Secret
      valuesKey: "common"
    - name: {{ .Release.Name }}-gitlab-values
      kind: Secret
      valuesKey: "defaults"
    - name: {{ .Release.Name }}-gitlab-values
      kind: Secret
      valuesKey: "overlays"

  {{- if or .Values.packages.addons.gatekeeper.enabled .Values.packages.core.istio.enabled .Values.packages.core.kyvernoPolicies.enabled .Values.packages.addons.monitoring.enabled }}
  dependsOn:
  {{- if .Values.packages.addons.gatekeeper.enabled }}
  - name: gatekeeper
    namespace: {{ .Release.Namespace }}
  {{- end }}
  {{- if .Values.packages.core.istio.enabled }}
  - name: istio
    namespace: {{ .Release.Namespace }}
  {{- end }}
  {{- if .Values.packages.core.kyvernoPolicies.enabled }}
  - name: kyverno-policies
    namespace: {{ .Release.Namespace }}
  {{- end }}
  {{- if .Values.packages.addons.monitoring.enabled }}
  - name: monitoring
    namespace: {{ .Release.Namespace }}
  {{- end }}
  {{- end }}
{{- end }}
