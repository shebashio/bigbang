{{- $fluxSettingsVault := merge .Values.packages.addons.vault.flux .Values.flux -}}

{{- if .Values.packages.addons.vault.enabled }}
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: vault
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/name: vault
    app.kubernetes.io/component: "security-tools"
    {{- include "commonLabels" . | nindent 4}}
  annotations:
    checksum/bigbang-values: {{ include (print $.Template.BasePath "/vault/values.yaml") . | sha256sum }}
spec:
  targetNamespace: vault
  driftDetection:
    mode: warn
  chart:
    spec:
      {{- if eq .Values.packages.addons.vault.sourceType "git" }}
      chart: {{ .Values.packages.addons.vault.git.path | default "./chart" }}
      sourceRef:
        kind: GitRepository
        name: vault
        namespace: {{ .Release.Namespace }}
      {{- else }}
      chart: {{ .Values.packages.addons.vault.helmRepo.chartName }}
      version: {{ .Values.packages.addons.vault.helmRepo.tag }}
      sourceRef:
        kind: HelmRepository
        name: {{ .Values.packages.addons.vault.helmRepo.repoName }}
        namespace: {{ .Release.Namespace }}
      {{- $repoType := include "getRepoType" (dict "repoName" .Values.packages.addons.vault.helmRepo.repoName "allRepos" $.Values.helmRepositories) -}}
      {{- if (and .Values.packages.addons.vault.helmRepo.cosignVerify (eq $repoType "oci")) }} # Needs to be an OCI repo
      verify:
        provider: cosign
        secretRef:
          name: {{ printf "%s-cosign-pub" .Values.packages.addons.vault.helmRepo.repoName }}
      {{- end }}
      {{- end }}
      interval: 5m

  {{- toYaml $fluxSettingsVault | nindent 2 }}
  
  {{- if .Values.packages.addons.vault.postRenderers }}
  postRenderers:
  {{ toYaml .Values.packages.addons.vault.postRenderers | nindent 4 }}
  {{- end }}
  valuesFrom:
    - name: {{ .Release.Name }}-vault-values
      kind: Secret
      valuesKey: "common"
    - name: {{ .Release.Name }}-vault-values
      kind: Secret
      valuesKey: "defaults"
    - name: {{ .Release.Name }}-vault-values
      kind: Secret
      valuesKey: "overlays"

  # TODO: DRY this up
  {{- if or .Values.packages.core.istio.enabled (dig "values" "minio" "enabled" false .Values.packages.addons.vault ) }}
  dependsOn:
    {{- if .Values.packages.core.istio.enabled }}
    - name: istio
      namespace: {{ .Release.Namespace }}
    {{- end }}

    {{- if (dig "values" "minio" "enabled" false .Values.packages.addons.vault) }}
    - name: minio-operator
      namespace: {{ $.Release.Namespace }}      
    {{- end }}
  {{- end }}
{{- end }}
