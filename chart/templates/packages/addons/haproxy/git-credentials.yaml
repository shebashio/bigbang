{{- $gitCredsSecretDict := dict
  "name" "haproxy"
  "targetScope" .Values.packages.addons.proxy
  "releaseName" .Release.Name
  "releaseNamespace" .Release.Namespace
}}
{{- include "gitCredsSecret" $gitCredsSecretDict | nindent 0 -}}
