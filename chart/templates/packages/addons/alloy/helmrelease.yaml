{{- $fluxSettingsMonitoring := merge .Values.packages.addons.alloy.flux .Values.flux -}}
{{- if .Values.packages.addons.alloy.enabled }}
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: alloy
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/name: alloy
    {{- include "commonLabels" . | nindent 4}}
  annotations:
    checksum/bigbang-values: {{ include (print $.Template.BasePath "/alloy/values.yaml") . | sha256sum }}
spec:
  targetNamespace: alloy
  releaseName: alloy
  chart:
    spec:
      {{- if eq .Values.packages.addons.alloy.sourceType "git" }}
      chart: {{ .Values.packages.addons.alloy.git.path | default "./chart" }}
      sourceRef:
        kind: GitRepository
        name: alloy
        namespace: {{ .Release.Namespace }}
      {{- else }}
      chart: {{ .Values.packages.addons.alloy.helmRepo.chartName }}
      version: {{ .Values.packages.addons.alloy.helmRepo.tag }}
      sourceRef:
        kind: HelmRepository
        name: {{ .Values.packages.addons.alloy.helmRepo.repoName }}
        namespace: {{ .Release.Namespace }}
      {{- $repoType := include "getRepoType" (dict "repoName" .Values.packages.addons.alloy.helmRepo.repoName "allRepos" $.Values.helmRepositories) -}}
      {{- if (and .Values.packages.addons.alloy.helmRepo.cosignVerify (eq $repoType "oci")) }} # Needs to be an OCI repo
      verify:
        provider: cosign
        secretRef:
          name: {{ printf "%s-cosign-pub" .Values.packages.addons.alloy.helmRepo.repoName }}
      {{- end }}
      {{- end }}
      interval: 5m

  {{- toYaml $fluxSettingsMonitoring | nindent 2 }}

  {{- if .Values.packages.addons.alloy.postRenderers }}
  postRenderers:
  {{ toYaml .Values.packages.addons.alloy.postRenderers | nindent 4 }}
  {{- end }}
  valuesFrom:
    - name: {{ .Release.Name }}-alloy-values
      kind: Secret
      valuesKey: "common"
    - name: {{ .Release.Name }}-alloy-values
      kind: Secret
      valuesKey: "defaults"
    - name: {{ .Release.Name }}-alloy-values
      kind: Secret
      valuesKey: "overlays"

  # TODO: DRY this up
  {{- if or .Values.packages.addons.gatekeeper.enabled .Values.packages.core.istio.enabled .Values.packages.core.kyvernoPolicies.enabled .Values.packages.addons.monitoring.enabled }}
  dependsOn:
  {{- if .Values.packages.core.istio.enabled }}
    - name: istio
      namespace: {{ .Release.Namespace }}
  {{- end }}
  {{- if .Values.packages.addons.gatekeeper.enabled }}
    - name: gatekeeper
      namespace: {{ .Release.Namespace }}
  {{- end }}
  {{- if .Values.packages.core.kyvernoPolicies.enabled }}
    - name: kyverno-policies
      namespace: {{ .Release.Namespace }}
  {{- end }}
  {{- end }}
{{- end }}

