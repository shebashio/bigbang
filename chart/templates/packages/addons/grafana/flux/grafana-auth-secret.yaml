{{- if and .Values.packages.addons.monitoring.enabled (dig "grafana" "enabled" true .Values.packages.addons.monitoring.values) }}
apiVersion: v1
kind: Secret
metadata:
  name: grafana-flux-auth
  namespace: monitoring
  labels:
    app.kubernetes.io/name: monitoring
    app.kubernetes.io/component: "core"
    {{- include "commonLabels" . | nindent 4}}
type: kubernetes.io/opaque
stringData:
{{- if (dig "grafana" "admin" "existingSecret" "" .Values.packages.addons.monitoring.values) }}
  {{- $passwordKey := (dig "grafana" "admin" "passwordKey" "admin-password" .Values.packages.addons.monitoring.values) }}
  {{- $userKey := (dig "grafana" "admin" "userKey" "admin-user" .Values.packages.addons.monitoring.values) }}
  {{- with lookup "v1" "Secret" "monitoring" .Values.packages.addons.monitoring.Values.packages.addons.grafana.admin.existingSecret }}
  username: {{ (get .data $userKey | b64dec) | default "admin" }}
  password: {{ (get .data $passwordKey | b64dec) | default "prom-operator" }}
  {{- end }}
{{- else }}
  username: {{ dig "grafana" "adminUser" "admin" .Values.packages.addons.monitoring.values }}
  password: {{ dig "grafana" "adminPassword" "prom-operator" .Values.packages.addons.monitoring.values }}
{{- end }}
{{- end }}