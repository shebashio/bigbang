{{- $fluxSettingsMonitoring := merge .Values.packages.addons.monitoring.flux .Values.flux -}}
{{- if .Values.packages.addons.monitoring.enabled }}
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: monitoring
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/name: monitoring
    app.kubernetes.io/component: "core"
    {{- include "commonLabels" . | nindent 4}}
  annotations:
    checksum/bigbang-values: {{ include (print $.Template.BasePath "/monitoring/values.yaml") . | sha256sum }}
spec:
  targetNamespace: monitoring
  chart:
    spec:
      {{- if eq .Values.packages.addons.monitoring.sourceType "git" }}
      chart: {{ .Values.packages.addons.monitoring.git.path | default "./chart" }}
      sourceRef:
        kind: GitRepository
        name: monitoring
        namespace: {{ .Release.Namespace }}
      {{- else }}
      chart: {{ .Values.packages.addons.monitoring.helmRepo.chartName }}
      version: {{ .Values.packages.addons.monitoring.helmRepo.tag }}
      sourceRef:
        kind: HelmRepository
        name: {{ .Values.packages.addons.monitoring.helmRepo.repoName }}
        namespace: {{ .Release.Namespace }}
      {{- $repoType := include "getRepoType" (dict "repoName" .Values.packages.addons.monitoring.helmRepo.repoName "allRepos" $.Values.helmRepositories) -}}
      {{- if (and .Values.packages.addons.monitoring.helmRepo.cosignVerify (eq $repoType "oci")) }} # Needs to be an OCI repo
      verify:
        provider: cosign
        secretRef:
          name: {{ printf "%s-cosign-pub" .Values.packages.addons.monitoring.helmRepo.repoName }}
      {{- end }}
      {{- end }}
      interval: 5m

  {{- toYaml $fluxSettingsMonitoring | nindent 2 }}

  {{- if .Values.packages.addons.monitoring.postRenderers }}
  postRenderers:
  {{ toYaml .Values.packages.addons.monitoring.postRenderers | nindent 4 }}
  {{- end }}
  valuesFrom:
    - name: {{ .Release.Name }}-monitoring-values
      kind: Secret
      valuesKey: "common"
    - name: {{ .Release.Name }}-monitoring-values
      kind: Secret
      valuesKey: "defaults"
    - name: {{ .Release.Name }}-monitoring-values
      kind: Secret
      valuesKey: "overlays"

  # TODO: DRY this up
  {{- if or .Values.packages.addons.gatekeeper.enabled (include "istioEnabled" .) .Values.packages.core.kyvernoPolicies.enabled .Values.packages.addons.vault.enabled }}
  dependsOn:
  {{- if .Values.packages.core.istio.enabled }}
    - name: istio
      namespace: {{ .Release.Namespace }}
  {{- end }}
  {{- if .Values.packages.core.istioCore.enabled }}
    - name: istio-core
      namespace: {{ .Release.Namespace }}
  {{- end }}
  {{- if .Values.packages.addons.gatekeeper.enabled }}
    - name: gatekeeper
      namespace: {{ .Release.Namespace }}
  {{- end }}
  {{- if .Values.packages.core.kyvernoPolicies.enabled }}
    - name: kyverno-policies
      namespace: {{ .Release.Namespace }}
  {{- end }}
  {{- if .Values.packages.addons.vault.enabled }}
    - name: vault
      namespace: {{ .Release.Namespace }}
  {{- end }}
  {{- end }}
{{- end }}
