{{- if .Values.packages.addons.loki.enabled }}
{{- include "values-secret" (dict "root" $ "package" .Values.packages.addons.loki "name" "loki" "defaults" (include "bigbang.defaults.loki" .)) }}
{{- end }}

{{- define "bigbang.defaults.loki" -}}
# hostname is deprecated and replaced with domain. But if hostname exists then use it.
{{- $domainName := default .Values.domain .Values.hostname }}
hostname: {{ $domainName }}
domain: {{ $domainName }}

clusterName: ""

openshift: {{ .Values.openshift }}

istio:
  enabled: {{ include "istioEnabled" . }}
  {{- if or 
      (dig "hardened" "enabled" false .Values.packages.core.istio.values)
      (dig "istio" "hardened" "enabled" false .Values.packages.addons.monitoring.values)
      (dig "istio" "hardened" "enabled" false .Values.packages.addons.authservice.values)
      (dig "istio" "hardened" "enabled" false .Values.packages.addons.grafana.values)
      (dig "istio" "hardened" "enabled" false .Values.packages.addons.loki.values)
      (dig "istio" "hardened" "enabled" false .Values.packages.addons.eckOperator.values)
      (dig "istio" "hardened" "enabled" false .Values.packages.addons.elasticsearchKibana.values)
  }}
  hardened:
    enabled: true
    monitoring:
      enabled: {{ .Values.packages.addons.monitoring.enabled }}
    promtail:
      enabled: {{ .Values.packages.addons.promtail.enabled }}
    fluentbit:
      enabled: {{ .Values.packages.addons.fluentbit.enabled }}
    minioOperator:
      enabled: {{ .Values.packages.addons.minioOperator.enabled }}
  {{- end }}
  loki:
    enabled: true
    gateways:
    - {{ include "istioGatewayNamespace" . }}/{{ default (include "istioPublicGateway" . ) }}

imagePullSecrets:
  - name: private-registry

image:
  pullPolicy: {{ .Values.imagePullPolicy }}

networkPolicies:
  enabled: {{ .Values.networkPolicies.enabled }}
  controlPlaneCidr: {{ .Values.networkPolicies.controlPlaneCidr }}
  istioNamespaceSelector:
  {{ include "istioNamespaceSelector" . | nindent 4 }}

monitoring:
  enabled: {{ .Values.packages.addons.monitoring.enabled }}
  serviceMonitor:
    enabled: {{ .Values.packages.addons.monitoring.enabled }}
    # conditional passes only for default istio: enabled, mTLS: SCRICT
    {{- if and (include "istioEnabled" . ) (eq (dig "istio" "mtls" "mode" "STRICT" .Values.packages.addons.loki.values) "STRICT") }}
    scheme: https
    tlsConfig:
      caFile: /etc/prom-certs/root-cert.pem
      certFile: /etc/prom-certs/cert-chain.pem
      keyFile: /etc/prom-certs/key.pem
      insecureSkipVerify: true  # Prometheus does not support Istio security naming, thus skip verifying target pod certificate
    {{- end }}
  rules:
    enabled: true
    alerting: true

{{- $disableDefaultFLB := dig "additionalOutputs" "disableDefault" false .Values.packages.addons.fluentbit.values }}
{{- if and .Values.packages.addons.fluentbit.enabled (not $disableDefaultFLB) }}
fluentbit:
  enabled: true
{{- end }}

{{- if .Values.packages.addons.promtail.enabled }}
promtail:
  enabled: true
{{- end }}

loki:
  extraMemberlistConfig:
    bind_addr:
      - ${MY_POD_IP}
  storage:
    {{- if (and (eq .Values.packages.addons.loki.strategy "monolith") (not (dig "minio" "enabled" false .Values.packages.addons.loki.values))) }}
    type: "filesystem"
    {{- else }}
    type: "s3"
    bucketNames:
      {{- toYaml .Values.packages.addons.loki.objectStorage.bucketNames | nindent 6 }}
    s3:
      endpoint: {{ .Values.packages.addons.loki.objectStorage.endpoint }}
      region: {{ .Values.packages.addons.loki.objectStorage.region }}
      accessKeyId: {{ .Values.packages.addons.loki.objectStorage.accessKey }}
      secretAccessKey: {{ .Values.packages.addons.loki.objectStorage.accessSecret }}
    {{- end }}
  {{- if (and (eq .Values.packages.addons.loki.strategy "monolith") (not (dig "minio" "enabled" false .Values.packages.addons.loki.values))) }}
  rulerConfig:
    storage:
      type: local
  storage_config:
    boltdb_shipper:
      active_index_directory: /var/loki/boltdb-shipper-active
      cache_location: /var/loki/boltdb-shipper-cache
      cache_ttl: 24h
    filesystem:
      directory: /var/loki/chunks
  {{- end }}
  {{- if include "istioEnabled" . }}
  podAnnotations:
    {{ include "istioAnnotation" . }}
  {{- end }}

{{- if (eq .Values.packages.addons.loki.strategy "monolith") }}
deploymentMode: SingleBinary
singleBinary:
  replicas: 1
  extraArgs:
    - -config.expand-env=true
  extraEnv:
    - name: MY_POD_IP
      valueFrom:
        fieldRef:
          fieldPath: status.podIP
{{- end }}

{{- if (eq .Values.packages.addons.loki.strategy "scalable") }}
deploymentMode: SimpleScalable
singleBinary:
  replicas: 0
read:
  replicas: 3
  extraArgs:
    - -config.expand-env=true
  extraEnv:
    - name: MY_POD_IP
      valueFrom:
        fieldRef:
          fieldPath: status.podIP
write:
  replicas: 3
  extraArgs:
    - -config.expand-env=true
  extraEnv:
    - name: MY_POD_IP
      valueFrom:
        fieldRef:
          fieldPath: status.podIP
backend:
  replicas: 3
  extraArgs:
    - -config.expand-env=true
  extraEnv:
    - name: MY_POD_IP
      valueFrom:
        fieldRef:
          fieldPath: status.podIP
gateway:
  enabled: true
  extraArgs:
    - -config.expand-env=true
  extraEnv:
    - name: MY_POD_IP
      valueFrom:
        fieldRef:
          fieldPath: status.podIP
{{- end }}

# Allows users to deploy distributed mode but calls out that we do not support it
{{- if (eq .Values.packages.addons.loki.strategy "distributed") }}
deploymentMode: Distributed
{{- end -}}

{{- end -}}
