{{- if and (eq .Values.gatewayAPI.sourceType "git") (not .Values.offline) (eq (include "gatewayAPIEnabled" .) "true") }}
{{- include "bigbang.gitRepository" (dict "name" "gateway-api" "gitCredsName" "gatewayAPI" "git" .Values.gatewayAPI.git "component" "core" "root" .) }}
{{- end }}
